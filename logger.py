class Logger:

    def log_transaction(self, order, int):
        self.accept_order_pizza = self.pizza_price (10)
        self.accept_order_pasta = order
        self.accept_order_salad = order
        self.location_number = ()
        pizza_price = (10)
        pasta_price = (12)
        salad_price = (8)

        # transaction_count(int)
        # daily_sales(int)